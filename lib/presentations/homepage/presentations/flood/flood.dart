import 'package:flutter/material.dart';

class Flood extends StatelessWidget {
  const Flood({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO Buat style ini sesuai dengan banjir
    return const Center(
      child: Text("banjir"),
    );
  }
}
